### Protocol



A network protocol is a set of established data structures that dictate how devices exchange data across networks.

### TCP vs UDP



TCP is a protocol that ensures all data is being transferred for a seamless connection. UDP is a protocol that just sends data without checking if the other end has received it.

